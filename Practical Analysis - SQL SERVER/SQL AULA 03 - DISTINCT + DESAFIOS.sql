-- seleciona os primeiros nomes únicos (não seleciona os duplicados)
SELECT DISTINCT FirstName
FROM Person.Person;

/* Quantos sobrenomes únicos temo nas tabela Person.Person */

SELECT DISTINCT LastName
FROM Person.Person;